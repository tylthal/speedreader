# === Speed Reader — Docker Environment ===

# Claude API key (Option A: pay-per-use)
# ANTHROPIC_API_KEY=sk-ant-...
# Leave commented for Option B: run 'claude login' inside container

# OpenAI API key (optional)
# OPENAI_API_KEY=sk-...

# Git identity
GIT_AUTHOR_NAME=Your Name
GIT_AUTHOR_EMAIL=your@email.com

# Host UID/GID — run 'id -u' and 'id -g' to get your values
HOST_UID=1000
HOST_GID=1000
